[pytest]
testpaths = tests
# Ensure the project root is importable no matter how pytest is launched:
# `python -m pytest` prepends the CWD to sys.path on its own, but the `pytest`
# console script (used by the verify compose service) does not.
pythonpath = .
